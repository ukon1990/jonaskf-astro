---
interface Props {
	date: Date;
	locale?: 'no' | 'en';
}

const { date, locale = 'en' } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString(locale === 'no' ? 'nb-NO' : 'en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
